# Us & Me 情侣生活 OS - 项目风险评估与优化建议

**生成日期**: 2026-01-28  
**版本**: 1.0  
**状态**: 核心功能开发完成 (Phase 1-5)

---

## 1. 架构与性能风险 (Architecture & Performance)

### 🔴 高风险: 轮询机制 (Polling)
- **现状**: `KitchenPage` 和 `BistroPage` 目前使用 `setInterval` 每 10 秒轮询一次数据库以获取最新订单。
- **风险**: 随着用户量增加，数据库读请求将呈指数级增长，可能导致 Supabase 配额耗尽或数据库响应变慢。
- **建议**: 迁移到 **Supabase Realtime** (WebSocket)。监听 `INSERT/UPDATE` 事件，仅在数据变化时更新 UI。

### 🟡 中风险: Supabase 强依赖
- **现状**: 身份验证、数据库、存储、边缘函数完全依赖 Supabase 生态。
- **风险**: 供应商锁定风险。如果 Supabase 服务中断或改变定价策略，迁移成本较高。
- **建议**: 保持核心业务逻辑在应用层（尽可能减少过度复杂的 SQL 存储过程），定期备份数据库数据。

---

## 2. 安全与数据隐私 (Security & Privacy)

### 🔴 高风险: RLS 策略复杂性
- **现状**: 使用了复杂的 RLS (Row Level Security) 策略，部分策略涉及跨表查询（如 `auth.uid() -> profiles -> family_id`）。
- **风险**: 复杂的 RLS 可能导致性能瓶颈，甚至出现递归死锁（已在开发中遇到并修复）。
- **建议**: 编写自动化测试脚本，专门测试不同用户角色的权限边界，确保“A家庭看不到B家庭的数据”。

### 🟡 中风险: 邀请码安全性
- **现状**: 邀请码是 7 位随机字符。
- **风险**: 存在被暴力破解加入陌生家庭的微小风险。
- **建议**: 增加邀请码的有效期（如 24 小时失效），或在输入错误 5 次后锁定账号。

---

## 3. 业务逻辑与经济系统 (Business Logic & Economy)

### 🟡 中风险: 积分通胀 (Points Inflation)
- **现状**: 用户可以通过做任务无限赚取积分，且没有“积分回收”机制（除了兑换菜品）。
- **风险**: 如果一方疯狂刷任务，导致积分数额巨大，菜品价格将失去调节意义。
- **建议**: 引入“每日积分上限”或“任务审批机制”（需对方确认任务完成才能获得积分）。

### 🟢 低风险: 并发一致性
- **现状**: 下单扣分逻辑使用了数据库事务 (`place_order` RPC)。
- **风险**: 理论上已解决并发扣分问题，但在网络极差情况下用户体验可能受影响。
- **建议**: 前端增加更强的防抖 (Debounce) 和乐观更新 (Optimistic UI) 回滚机制。

---

## 4. 可维护性与代码质量 (Maintainability)

### 🔴 高风险: SQL 脚本分散
- **现状**: 数据库 Schema 变更分散在 `v1` 到 `v11` 多个 SQL 文件中。
- **风险**: 新环境部署困难，难以追踪当前数据库的确切状态。
- **建议**: 引入数据库迁移工具 (如 Supabase CLI migrations 或 Prisma)，将所有 SQL 合并为一个可复现的 Schema。

### 🟡 中风险: TypeScript 类型定义
- **现状**: 前端 `types/index.ts` 是手动维护的。
- **风险**: 数据库表结构变更后，如果忘记更新前端类型，会导致运行时错误。
- **建议**: 使用 `supabase gen types typescript` 自动生成类型定义，确保端到端类型安全。

---

## 5. 总结与下一步计划

当前系统已具备完整MVP功能，核心流程（家庭-任务-积分-消费）已跑通。主要的风险在于**未来的扩展性**（轮询性能）和**运维便利性**（SQL管理）。

### 🚀 优先修复建议
1.  **合并 SQL**: 将 `v1-v11` 整理为一个干净的 `schema.sql`。
2.  **引入 Realtime**: 改造订单监听逻辑。
3.  **增加任务审批**: 防止单方面刷分。

---
*Generated by Trae AI Assistant*
